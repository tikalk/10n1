{
  "name": "1on1-nx",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "ui:dev": "nx run ui:dev",
    "ui:build": "nx run ui:build",
    "ds:dev": "nx run ds:dev",
    "ds:build": "nx run ds:build",
    "ds:start": "nx run @1on1/ds:start"
  },
  "private": true,
  "devDependencies": {
    "@types/react": "^18.2.3",
    "nx": "^16.0.3",
    "nx-cloud": "latest"
  },
  "workspaces": [
    "packages/*"
  ]
}
