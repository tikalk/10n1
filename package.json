{
  "name": "1on1-nx",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "ui:dev": "nx run ui:dev",
    "ui:build": "nx run ui:build",
    "ds:dev": "nx run ds:dev",
    "ds:build": "nx run ds:build"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "16.0.3",
    "nx-cloud": "latest"
  },
  "workspaces": [
    "packages/*"
  ]
}
